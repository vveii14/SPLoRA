# Proposal Draft

Main file: `structured_update_proposal.tex`

This draft is a formal proposal-style manuscript organized around:

- `Introduction`
- `Related Work`
- `Method`
- `Hypotheses`
- `Expected Contribution`

It is written to preserve the current project framing:

1. `Level 1`: componentized update modeling in the matrix setting
2. `Level 2`: structure-aware regularization
3. `Level 3`: component-aware optimization
4. `Extension`: matrix-to-tensor generalization

## Notes

- The current environment does not have `pdflatex` or `latexmk`, so the `.tex` file was not compiled locally.
- A few bibliography entries are intentionally conservative where full author lists were not verified in the repository state. If needed, the next step should be a reference-cleanup pass with exact BibTeX entries.
- The content is aligned with the existing survey conclusion in `.pipeline/docs/research_brief.json`: novelty should not rely on tensorization or sparsity alone, but on a unified structured-update framing.
